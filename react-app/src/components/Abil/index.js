import React, { useState } from 'react';
import { useDispatch } from "react-redux";
import 'abil.css'


const AbilDetailsCard = (         ) => {
	const dispatch = useDispatch();

	const [errors, setErrors] = useState([])


	return (
		<>

		</>
	)
}

export default AbilDetailsCard;